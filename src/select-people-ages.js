const { readPeopleFromPath } = require('./read-people');

//The `selectPeopleOfAges` function should have the following parameters:
//inputPath: of the .json file
//ageStart: starting age where to select people entries from
//ageEnd: ending age where to select people entries to


module.exports = {
    selectPeopleOfAges: selectPeopleOfAges
}